import 'dart:ui';

import 'package:flutter/material.dart';

mixin Palette {
  static const accent = Color(0xFF090446);
  static const primary = Colors.white;
  static const grey = Color(0xFF808A9F);
}
